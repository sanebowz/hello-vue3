{
  "name": "hello-vue3",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm-run-all build:*",
    "preview": "vite preview",
    "start": "npm run build && node server"
  },
  "keywords": [
    "heroku"
  ],
  "author": "sanebow",
  "dependencies": {
    "vue": "^3.3.4"
  },
  "engines": {
    "node": "18.17.1"
  }
}
